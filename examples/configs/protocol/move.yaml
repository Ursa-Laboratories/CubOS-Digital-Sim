# Sample protocol demonstrating all available commands.
# Load with: load_protocol_from_yaml("configs/protocol/protocol.sample.yaml")
# Execute with: protocol.run(ProtocolContext(board=board, deck=deck))

protocol:
  # ── Move ──────────────────────────────────────────────────────────
  - move:
      instrument: uvvis
      position: plate_1.A1
